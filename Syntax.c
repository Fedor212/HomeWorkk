
// Functions
int abss (int a)
{
	if (a>=0)	
	return a;
	return -a;
}

//  Cycles
for (int i=11; i<=a; i++)
{
	
}

//  Cycles
for (; i<=a;)  //Без некоторых аргументов
{
	
}

 // Ternarnik
 min = a%10 < min ? a%10 : min;
 
 sum==0 ? printf ("YES") : printf ("NO");
